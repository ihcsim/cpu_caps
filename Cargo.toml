[package]
name = "cpu_caps"
version = "0.1.0"
edition = "2024"

[dependencies]
quick-xml = { version = "0.39.0", features = [
  "serialize",
  "async-tokio",
  "encoding",
  "overlapped-lists",
] }
serde = { version = "1.0.228", features = ["derive"] }
